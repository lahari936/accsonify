# Backend runtime settings
# Comma-separated allowed frontend origins for CORS
FRONTEND_ORIGIN=https://accsonify-gukw.vercel.app

# Keep false in production so wrong mock predictions are never served
ALLOW_MOCK_MODE=false
